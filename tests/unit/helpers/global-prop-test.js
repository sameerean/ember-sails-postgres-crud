import {
  globalProp
} from 'ember-sails-postgres-crud/helpers/global-prop';

module('GlobalPropHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = globalProp(42);
  ok(result);
});
