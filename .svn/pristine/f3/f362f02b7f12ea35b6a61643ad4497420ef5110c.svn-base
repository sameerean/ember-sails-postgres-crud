export function initialize(_container, _app) {
  // application.inject('route', 'foo', 'service:foo');
      console.info("Inside TOP - " + this + ".initialize..... _container = " + _container + ", _app = " + _app);
      _app.set("myName", "ShameeeeerrrrRRRR");
}

export default {
  name: 'global-properties',
  initialize: function(_container, _app) {
      console.info("Inside " + this + ".initialize..... _container = " + _container + ", _app = " + _app);
      _app.set("myName", "Shameeeeer");
  }
};
